# Colyseus multiplayer server (local: pnpm server from repo root)
VITE_COLYSEUS_URL=ws://localhost:2567

# Production Colyseus (Render) — set after server deploy
# VITE_COLYSEUS_URL=wss://boomerang-fu-colyseus.onrender.com
